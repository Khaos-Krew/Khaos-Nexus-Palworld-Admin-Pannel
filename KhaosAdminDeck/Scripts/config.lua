return {
    -- Allows arbitrary slash commands from the UI. Keep false for initial testing.
    allow_raw = false,

    -- F9 opens the overlay; Ctrl+F5 saves the world.
    enable_hotkeys = true,

    verbose_logging = false,
    ipc_poll_ms = 250,
    status_refresh_ms = 2000
}
